#
# The module building tool
#
# Copyright (C) 2009 Nikolay V. Nemshilov aka St.
#

require 'rake'
require 'fileutils'
require File.dirname(__FILE__) + "/../../lib/frontcompiler/init.rb"

SLIDER_VERSION    = '1.0.0'
SLIDER_BUILD_DIR  = File.dirname(__FILE__) + "/../../build"
SLIDER_BUILD_NAME = 'right-slider'

SLIDER_FILES = %w{
  slider
  document
}

task :default => :build

desc "Builds the module"
task :build do
  puts "\n * Building Slider\n"
  
  FileUtils.mkdir_p SLIDER_BUILD_DIR
  
  # removing the old versions
  FileUtils.rm "#{SLIDER_BUILD_DIR}/#{SLIDER_BUILD_NAME}.js"     rescue nil
  FileUtils.rm "#{SLIDER_BUILD_DIR}/#{SLIDER_BUILD_NAME}-min.js" rescue nil
  FileUtils.rm "#{SLIDER_BUILD_DIR}/#{SLIDER_BUILD_NAME}-src.js" rescue nil
  
  @compiler = FrontCompiler.new
  
  @source   = SLIDER_FILES.collect do |filename|
    File.open("#{filename}.js").read
  end.join("\n\n")
  
  @source << "\n\n#{@compiler.inline_css(File.open('slider.css').read)}"
  
  @source.gsub! '#{version}', SLIDER_VERSION
  
  @header   = File.open("header.js").read
  
  # writting files
  File.open("#{SLIDER_BUILD_DIR}/#{SLIDER_BUILD_NAME}.js", "w") do |file|
    file.write @header
    file.write @compiler.compact_js(@source).create_self_build
  end
  
  File.open("#{SLIDER_BUILD_DIR}/#{SLIDER_BUILD_NAME}-min.js", "w") do |file|
    file.write @header
    file.write @compiler.compact_js(@source)
  end
  
  File.open("#{SLIDER_BUILD_DIR}/#{SLIDER_BUILD_NAME}-src.js", "w") do |file|
    file.write @header
    file.write "\n#{@source}"
  end
end